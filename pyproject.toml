[project]
name = "mini-vps-platform"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = ["libvirt-python==12.0.0", "pyyaml>=6.0.3"]

[tool.setuptools.package-data]
mini_vps = ["*.yaml"]
